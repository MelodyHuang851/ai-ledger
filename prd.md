# 一句账 AI 记账项目 PRD

## 1. 项目背景

### 用户痛点
传统记账 App 的核心问题：**记账时才发现超支，但钱已经花出去了**。用户需要的是“事前预警”而非“事后展示”。

### 核心洞察
记账的意义不是记录过去，而是改变下一笔。通过 AI 解析自然语言输入，在记账过程中即时反馈对预算的影响，并在月初就能预测月底是否会超支。

---

## 2. 原型设计思路与理念

### 设计理念
> **一句账：把“发现超支”从月底账单，提前到花钱之前。**

### 三层反馈机制
| 时刻 | 功能 | 解决什么痛点 |
|------|------|-------------|
| **事前** | 「今日还能花 ¥4,171」= 剩余预算 ÷ 剩余天数 | 花钱之前就知道安全额度 |
| **事中** | 记一笔立刻反馈：“占本月预算 2%，够再来 45 次” + 一键撤销 | 每笔消费都有即时冲击感 |
| **事后** | 直接用自然语言问：“这个月吃饭花了多少？”“月底会超吗？” | 复盘不用翻表格 |

### 节奏预测
按当月日均支出外推月底总支出，对比“支出进度 vs 时间进度”：
- 支出进度 > 时间进度 → 预警变色（黄/红）
- 月初第 3 天就能知道“按此速度月底将超 ¥380”

---

## 3. 技术选型与架构

### 技术栈
- **后端**：Node.js 22 零依赖（原生 http + fetch），JSON 文件存储
- **前端**：原生 JavaScript + Chart.js，单文件架构
- **AI 服务**：智谱 GLM 4.7-flash（免费模型）
- **部署**：本地启动，浏览器访问

### 架构原则
```
用户一句话
   │
   ▼
┌──────────────┐  一次 LLM 调用做意图路由
│  GLM 解析     │──▶ 记账   ──▶ 白名单校验 ──▶ 落库 ──▶ 本地计算预算冲击
│ (人话 → JSON) │──▶ 调预算 ──▶ 校验类目/金额 ──▶ 本地修改预算
│              │──▶ 提问   ──▶ 本地聚合统计 ──▶ LLM 只负责组织语言回答
└──────────────┘
```

### 关键技术决策
1. **LLM 边界清晰**：只做“人话 ⇄ 结构化”翻译，所有算术由本地确定性代码完成
   - 理由：LLM 算术不可靠、账目隐私、token 成本
2. **意图路由**：一个输入框、一次 LLM 调用，识别记账/调预算/提问三种意图
3. **兼容性兜底**：Node 14+ 兼容（无全局 fetch 时退回原生 https.request）
4. **容错机制**：LLM 输出白名单校验、日期格式归一化、时区处理

---

## 4. 项目特点

### 创新功能
1. **自然语言入口**：支持“午饭35，打车12”“把娱乐预算改成300”“这个月奶茶花了多少”等混合意图
2. **体感化反馈**：用“还能喝几杯奶茶”替代抽象数字，增强用户感知
3. **节奏预测**：支出进度跑赢时间进度时变色预警，月初就能预测超支
4. **一键撤销**：AI 解析结果可撤销，信任但可纠错

### 工程亮点
1. **零依赖启动**：Node 原生模块，无需 npm install
2. **本地优先**：账目数据不出户，计算本地完成
3. **防御性编程**：版本不匹配时显示指引而非崩溃
4. **启动脚本自愈**：自动清理旧进程，避免版本混跑

---

## 5. 页面渲染特点

### 设计语言
- **风格**：暖纸色底（#f6f4f0）、卡片式布局、低饱和类目色板
- **字体**：系统字体栈 + 等宽数字（tabular-nums）
- **动效**：进度条缓动、反馈卡片淡入、hover 状态

### 核心组件
1. **Hero 区域**
   - 本月支出大数字（¥550.35）
   - 支出进度条（绿色/黄色/红色）
   - 时间进度刻度线
   - 「今日还能花 ¥4,171」突出显示

2. **命令栏**
   - 大输入框（占满容器）
   - 示例 chips（一键填入）
   - AI 解析状态反馈

3. **预算红绿灯**
   - 类目进度条（颜色状态）
   - 节奏预测提示
   - 点击金额可编辑

4. **数据洞察**
   - 每日支出柱状图（带日均预算线）
   - 类目占比环图
   - 流水表格（hover 高亮）

### 响应式设计
- 移动端：Hero 变为单列、图表堆叠、网格最小宽度 150px
- 断点：720px 以下切换移动布局

---

## 6. 数据流程

### 记账流程
1. 用户输入 → 意图路由（记账/调预算/提问）
2. 记账：GLM 解析 → 白名单校验 → 本地落库 → 计算冲击反馈
3. 反馈：显示预算占比、剩余次数、一键撤销

### 查询流程
1. 用户提问 → 意图识别（查询）
2. 本地聚合统计（月度数据）
3. GLM 组织语言 → 自然语言回答

### 预测逻辑
- 日均支出 = 本月已花 / 已过天数
- 月底预测 = 日均支出 × 总天数
- 预警：预测值 > 预算 → 超支提醒

---

## 7. 用户体验设计

### 新手引导
- 首次启动：显示“今日还能花”概念说明
- 示例 chips：快速体验三种意图
- 错误提示：具体操作指引（如“请关闭旧窗口重新启动”）

### 操作反馈
- 即时反馈：记账后立即显示冲击
- 视觉提示：进度条颜色、数字变化
- 错误恢复：撤销按钮、重试机制

### 信息架构
- 优先级：今日还能花 > 预算状态 > 流水明细
- 扫描路径：Hero → 预算 → 数据 → 流水
- 快捷操作：Enter 记账、点击编辑预算

---

## 8. 技术规格

### 性能指标
- 启动时间：< 1 秒
- AI 响应：< 2 秒（网络正常时）
- 本地计算：< 100ms

### 安全与隐私
- 数据存储：本地 JSON 文件
- 网络传输：HTTPS（GLM API 调用）
- 敏感信息：账目明细不离开本地

### 兼容性
- 浏览器：Chrome 80+, Firefox 75+, Safari 13+
- Node.js：14.14+（含 https 兼容）
- 操作系统：Windows 10/11

---

## 9. 未来迭代方向

### 短期优化
- [ ] 多月账本对比
- [ ] 语音输入支持
- [ ] 预算建议算法（基于历史数据）

### 中期规划
- [ ] 数据导入导出
- [ ] 家庭账户共享
- [ ] 支出分类智能推荐

### 长期愿景
- [ ] 跨设备同步（加密 P2P）
- [ ] 消费习惯分析报告
- [ ] 预算自动调整建议

---

## 10. 成功指标

### 用户价值
- 预算超支率下降 50%
- 记账操作时长减少 70%
- 用户主动查询频率 > 3 次/周

### 技术指标
- AI 意图识别准确率 > 95%
- 本地计算零错误
- 启动成功率 100%

---

**文档版本**：v1.0  
**最后更新**：2025-08-30  
**维护者**：一句账团队